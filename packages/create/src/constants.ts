export const REQUIRED_NODE_VERSION = '>=20.0.0';

/**
 * The TypeScript version needs to pinned because minor versions often
 * introduce breaking changes.
 */
export const TYPESCRIPT_VERSION = '5.8.2';

// Hexos package versions (update at publish time)
export const HEXOS_VERSION = '0.3.2';

// Next.js and peer dependency versions
export const NEXTJS_VERSION = '15.1.0';
export const REACT_VERSION = '19.0.0';

// Timing constants (milliseconds)
export const SCAFFOLD_DELAY_MS = 300;

// Default project values
export const DEFAULT_PROJECT_VERSION = '0.1.0';

// Per-provider model defaults
export const DEFAULT_ANTHROPIC_MODEL = 'claude-sonnet-4-5-20250929';
export const DEFAULT_OPENAI_MODEL = 'gpt-4o-mini';
export const DEFAULT_OLLAMA_MODEL = 'llama3.2';
export const DEFAULT_OLLAMA_HOST = 'http://localhost:11434';
